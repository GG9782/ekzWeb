# DataSource configuration
spring:
    datasource:
        url : jdbc:mysql://10.41.34.24:3306/ekzdb24
        username : root
        password : G0ngK@i
        driver-class-name : com.mysql.cj.jdbc.Driver
# Druid configuration
        type : com.alibaba.druid.pool.DruidDataSource
        druid.initial-size : 5
        druid.max-active : 20
# devtools热部署
    devtools:
        restart:
            enabled: true  #设置开启热部署
            additional-paths: src/main/java #重启目录
            exclude: WEB-INF/**
ldap:
    host: 10.41.20.20
    port: 389
#    thymeleaf:
#        check-template : false
#        check-template-location : false
##文件上传的配置
#    servlet:
#        multipart:
#            max-file-size: 10MB
#            max-request-size: 100MB

shiro:
    loginUrl:/users/adLogin

mybatis-plus:
    type-aliases-package: com.ekz.ekzwebmp.domain.po
    global-config:
        db-config:
            id-type: INPUT
            map-underscore-to-camel-case: true

            logic-not-delete-value: 0  # 逻辑未删除值（默认为 1）
            logic-delete-value: 1      # 逻辑删除值（默认为 0）
            logic-delete-field: deleted   # 逻辑删除字段名（默认为 "deleted"）
    configuration:
        map-underscore-to-camel-case: true
#        default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
knife4j:
    enable: true
    openapi:
        title: 管理接口文档
        description: "管理接口文档"
        email:
        concat: KaiGong
        url:
        version: v1.0.0
        group:
            default:
                group-name: default
                api-rule: package
                api-rule-resources:
                    - com.ekz.ekzwebmp.controller
